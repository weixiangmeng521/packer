{
  "name": "packer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "devDependencies": {
    "@babel/cli": "^7.12.1",
    "@babel/core": "^7.12.3",
    "cssnano": "^4.1.10",
    "postcss": "^8.1.3",
    "postcss-cli": "^8.1.0",
    "postcss-css-variables": "^0.17.0",
    "postcss-image-inliner": "^4.0.3",
    "postcss-preset-env": "^6.7.0"
  },
  "scripts": {
    "css": "npx postcss .cache/.tmp -u postcss-preset-env -u postcss-css-variables -u postcss-image-inliner -r --no-map",
    "js": "npx babel .cache --out-file ./cache/.tmp --presets=@babel/preset-env"
  },
  "browserslist": [
    "last 5 version",
    "ie 9",
    "> 1%"
  ],
  "babel":{
    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "postcss-url": "^10.0.0"
  }
}
